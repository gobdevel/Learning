apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dynamic-pv-claim
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 8Gi
  storageClassName: standard

