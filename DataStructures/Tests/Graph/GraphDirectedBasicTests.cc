#include <iostream>

#include "DirectedGraph.h"

/*
int main() {
    DirectedGraph<int> g(5);
    g.addEdge(0, 1);
    g.addEdge(0, 2);
    g.addEdge(1, 2);
    g.addEdge(1, 4);
    g.addEdge(1, 3);
    g.addEdge(2, 3);
    g.addEdge(3, 4);

    g.BFS(0, [](int node) { std::cout << node << ", "; });
    std::cout << "\n";
    std::cout << "Path Exists : " << g.isPathExists(0, 4) << "\n";
    g.printAllPath(0, 4);
}
*/
